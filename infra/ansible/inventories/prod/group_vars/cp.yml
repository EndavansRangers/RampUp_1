---
# Control Plane specific variables

# This node will be the first control plane
is_primary_control_plane: "{{ inventory_hostname == groups['cp'][0] }}"

# Certificate key for joining additional control planes
# This will be generated by the primary CP and shared
certificate_key: ""
